# alist_win 验收报告（M7）

> 日期：2026-09-06 ｜ 版本：1.0.0 ｜ 内核：alist v3.64.0（AGPL-3.0）
> 范围：M0–M7 全量交付。M0–M3 见此前开发记录，本报告覆盖 M4–M7 并给出整体验收结论。

---

## 1. 交付物清单

| 产物 | 位置 | 大小 | 说明 |
| --- | --- | --- | --- |
| 安装包 | `alist_win/dist/alist_win-setup-1.0.0.exe` | 39.5 MB | NSIS，per-user 安装到 `%LOCALAPPDATA%\Programs\alist_win` |
| 便携包 | `alist_win/dist/alist_win-portable-1.0.0.zip` | 49.9 MB | 解压即用，含 `portable.ini` |
| 应用本体 | `alist_win/app/build/bin/app.exe` | 11.6 MB | Wails v2 + WebView2（download 策略） |
| 源码 | `alist_win/app/` | — | Go 后端 + Vue3/TS 前端 |

## 2. M4–M7 完成情况

### M4 托盘与窗口 ✅（代码完成，GUI 待真机验证）
- `internal/tray`：托盘图标 + 菜单树（启动/停止/重启/浏览器打开/地址/日志/数据目录/开机自启/关于/退出），菜单文案随运行状态动态刷新。
- 关闭窗口 = 隐藏到托盘（`OnBeforeClose` 拦截）；仅托盘"退出"真正停止服务并退出。
- 单实例：`internal/singleinstance`（Windows 命名互斥量），重复启动唤起已有窗口后退出。

### M5 设置与关于 ✅
- 版本信息：产品 1.0.0 + 内核 v3.64.0，"关于"弹窗含许可证/上游地址/第三方声明/免责声明。
- 前端：日志级别过滤、关键字搜索、自动滚动、导出 `.log`、清空；地址一键复制（02:11 变更后管理员信息卡片已移除）。
- 开机自启（HKCU Run）+ `--silent` 静默启动到托盘。

### M6 打包 ✅
- 自定义 NSIS 脚本（UTF-8 BOM）：per-user 免管理员安装、桌面/开始菜单快捷方式、卸载项注册（HKCU）、卸载清理自启注册表并可选删除用户数据。
- 便携 zip：`portable.ini` 触发便携模式，数据落 exe 同目录。
- WebView2 策略：`download`（首次联网下载运行时）。
- 图标：`build/appicon.png` → 生成 PNG-in-ICO（`internal/trayicon/icon.ico`），exe / 托盘 / 安装包共用。
- 密码存储：**已随 02:11 变更移除**（原 AES-256-GCM 方案废弃）。程序不保存任何密码，首启注入与一键重置均固定 admin/admin。

### M7 验收结果

**本环境已验证 ✅**
| 项 | 结果 |
| --- | --- |
| `go vet` 全仓 | 通过 |
| 冒烟测试（密码模型变更后回归） | **PASS**：内核定位→首启密码注入(admin)→日志捕获→健康检查(5244)→ResetAdmin(停服→admin set admin→重启→running) |
| `wails build` 完整链路 | 通过（绑定生成 + vue-tsc + vite + Go 编译） |
| NSIS 编译 | 通过（脚本含中文，UTF-8 BOM 已核验） |
| 便携 zip 结构 | 6 文件齐全：exe / portable.ini / LICENSE.txt / README.txt / bin\alist.exe / bin\LICENSE-alist.txt |
| AGPL 随附物 | bin\LICENSE-alist.txt 为 AGPL-3.0 全文（34.5KB）；LICENSE.txt 含第三方组件声明与源码获取要约 |

**需真机（有显示器桌面）验证 ⚠️** — 沙箱无显示环境，无法渲染 WebView2
| # | 项 | 风险 | 回退 |
| --- | --- | --- | --- |
| V1 | 托盘图标显示与菜单交互（R2：systray 与 Wails 消息循环共存） | 中 | 换 `energye/systray` |
| V2 | 关闭窗口 → 托盘；托盘"退出"真正结束 | 低 | — |
| V3 | 单实例：二次启动唤起已有窗口 | 低 | 去掉唤起，仅退出 |
| V4 | `--silent` 开机静默启动到托盘 | 低 | — |
| V5 | WebView2 download 策略首次联网下载运行时 | 低 | 改 `embed`（+150MB） |
| V6 | SmartScreen 提示与"仍要运行" | 低 | 代码签名（用户已决策不做） |

**真机手动测试清单**
1. 运行 `dist/stage-install/alist_win.exe`（或安装后从开始菜单）：确认窗口出现、状态徽章变化。
2. 点击"启动"→ 约 2–5s 后变"运行中"，浏览器打开 `http://127.0.0.1:5244` 可登录（账号密码均为 admin）。
3. 关闭窗口 → 确认托盘图标存在、alist 仍在运行（任务管理器有 alist.exe）。
4. 托盘右键逐项测试菜单；"退出"后确认 alist.exe 消失。
5. 再次双击 exe → 应唤起已有窗口而非新开进程。
6. 勾选"开机自启"→ 重启机器 → 确认静默启动且托盘可见。
7. 卸载：控制面板卸载 → 确认快捷方式/注册表项清理、数据目录询问弹窗。

## 3. 合规状态（需法务最终确认）
- AGPL-3.0 全文已随分发（安装包与便携包均有）。
- LICENSE.txt 提供源码获取要约（上游仓库地址 + 版本号在"关于"页可见）。
- 未修改 alist 源码（F2/C2：无修改披露义务）。
- ⚠️ "打包分发 alist 本体"的 AGPL 义务边界与 "AList" 商标使用，仍需法务确认后再对外分发。

## 4. 遗留与后续（V2 候选）
- 内核在线更新、挂载为本地硬盘（WebDAV/WinFsp）、产品自更新、内嵌管理界面（Q6）。
- 密码安全边界：程序不保存任何密码、无本地加密存储；首启与"重置密码"均固定 admin/admin（用户在网页端改过密码后，用重置按钮一键还原）。
- 安装前 taskkill 策略为静默强杀运行中的实例（V1 简化），可改为优雅提示。

## 5. 变更记录

### 2026-09-06 01:27 反馈修复（版本号不变，产物已重新生成）
1. **双主题**：新增 `frontend/src/theme.ts`（浅色/深色/自动三态，localStorage 持久化，自动模式实时跟随系统 `prefers-color-scheme`）；App.vue 全量迁移到 CSS 变量并双主题定义；浅色主题加深了此前对比度不足的文字色（muted #8a9099→#5f6673、badge/code 同步调整）；顶栏新增 ☀/☾/◐ 三态切换。
2. **隐藏 CMD 窗口**：`alist.exe server` 与 `admin set` 子命令的 spawn 增加 `SysProcAttr.HideWindow: true`，修复 GUI 父进程拉起控制台子进程时弹出黑色 CMD 窗口（用户误关会杀掉服务）。
3. 回归：冒烟测试 PASS；`wails build` + NSIS + 便携 zip 全部重新打包成功。
4. 工程侧：package.sh 清理逻辑由 `rm -rf` 改为 `mv` 到 `.trash-*` 隔离目录（规避沙箱安全删除钩子）；NSIS BOM 插入改为幂等（双 BOM 会导致 makensis "Invalid command"）。

### 2026-09-06 01:33 反馈修复
1. 密码仅在首次初始化（config.json 不存在）时注入，日常启动/重启不碰数据库；UI 明确标注"不会重置账号密码"。
2. 新增"自动启动服务"设置（默认开），持久化到 `desktop-settings.json`。
3. 冒烟测试在 5244 端口被占用（用户开着实例）时自动 Skip，避免假阳性。

### 2026-09-06 02:11 反馈修复（版本号不变，产物已重新生成）
1. **移除主界面"管理员"卡片**：不再显示/保存/加密任何密码（`desktop-admin.json` 与 AES-GCM 存储整体删除，`password.go` 仅保留注册表自启逻辑并更名 `autostart.go`）。
2. **新增"重置密码"按钮**（状态卡片按钮行，数据目录旁）：一键将账号密码重置为 **admin / admin**（两段式确认防误触，运行中服务会短暂重启）。实现：`alist admin set admin`（内核按角色定位管理员）；若用户名曾于网页端被改名，密码仍会重置，用户名需在网页端改回。首次初始化注入的密码也从随机生成改为固定 `admin`。
3. **修复"自动启动服务"不生效**：根因是上一轮对 `app.go startup` 的编辑丢失，设置从未被读取（仅 `--silent` 分支启动服务）。现改为 `--silent || AutoStartService` 时**延迟 1 秒**再启动 alist（避免与主窗口/托盘初始化抢占资源）。
4. 回归：`go vet` 通过；冒烟测试 PASS（首启注入 admin → ResetAdmin 停服重置 → 重启 running）；绑定文件已同步（GetAdminInfo/ResetPassword → ResetAdmin）。
